# Copy to .env.local for local builds, or set in the Vercel dashboard.
# Point this at your deployed backend, including the /api suffix.
# Leave unset in dev — Vite proxies /api to localhost:5000 (see vite.config.js).
VITE_API_URL=https://your-backend.onrender.com/api
